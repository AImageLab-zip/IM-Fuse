[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "MiMoSe"
version = "0.0.2"
description = "This repository provides a unified benchmark for brain tumor segmentation under missing modality conditions, built on the BraTS 2023 dataset. It offers a standardized framework to fairly reproduce, evaluate, and compare state-of-the-art methods designed for incomplete multimodal medical imaging."
readme = "README.md"
requires-python = "==3.12.*"
license = { text = "MIT" }
authors = [
  { name = "Omar Carpentiero", email = "omar.carpentiero@unimore.it" },
  { name = "Kevin Marchesini", email = "kevin.marchesini@unimore.it" },
  { name = "Vittorio Pipoli", email = "vittorio.pipoli@unimore.it" },
  { name = "Alessia Saporita", email = "alessia.saporita@unimore.it" },
  { name = "Costantino Grana", email = "costantino.grana@unimore.it" },
    { name = "Federico Bolelli", email = "federico.bolelli@unimore.it" }
]
dependencies = [
    "causal-conv1d>=1.4.0",
    "mamba-ssm",
    "dynamic-network-architectures>=0.4.3",
    "fastapi>=0.128.0",
    "python-dotenv>=1.2.1",
    "python-multipart>=0.0.20",
    "uvicorn>=0.40.0",
    "h5py>=3.16.0",
    "huggingface_hub>=0.34.0",
    "matplotlib>=3.10.9",
    "medpy>=0.5.2",
    "ninja>=1.13.0",
    "numpy>=2.4.3",
    "openpyxl>=3.1.5",
    "packaging>=26.0",
    "pandas>=3.0.2",
    "prompt-toolkit>=3.0.52",
    "ptflops>=0.7.5",
    "pyyaml>=6.0.3",
    "rich>=14.3.3",
    "safetensors>=0.6.2",
    "scikit-learn>=1.8.0",
    "scipy>=1.17.1",
    "thop>=0.1.1.post2209072238",
    "torch==2.9.1",
    "torchvision==0.24.1",
    "tqdm>=4.67.3",
    "ttkbootstrap>=1.20.2",
    "typer>=0.24.1",
    "wandb>=0.25.1",
    "wheel>=0.46.3",
]

[project.optional-dependencies]
dev = [
  "pytest>=8",
  "ruff>=0.6",
  "mypy>=1.11",
]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.package-data]
mimose = [
  "data/dummy_brain.npz",
  "data/configs/*.yaml",
  "data/config_templates/*.yaml",
  "data/splits/**/*.csv",
  "data/splits/**/*.json",
  "data/splits/**/*.py",
  "data/splits/**/*.txt",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.uv]
environments = ["sys_platform == 'linux'"]

[tool.uv.sources]
causal-conv1d = { url = "https://github.com/AImageLab-zip/MiMoSe/releases/download/wheels-cu128-py312-v1/causal_conv1d-1.6.2.post1-cp312-cp312-linux_x86_64.whl" }
mamba-ssm = { url = "https://github.com/AImageLab-zip/MiMoSe/releases/download/wheels-cu128-py312-v1/mamba_ssm-2.3.2.post1-cp312-cp312-linux_x86_64.whl" }
torch = { url = "https://github.com/homarone00/torch-pascal-builds/releases/download/v2.9.1-pascal-r2/torch-2.9.1-cp312-cp312-linux_x86_64.whl" }

[tool.ruff]
line-length = 88
target-version = "py313"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.mypy]
python_version = "3.12"
strict = true

[dependency-groups]
dev = [
    "nodeocc>=1.0.26",
]

[project.scripts]
mimose = "mimose.cli:app"
mimose-gui = "mimose.gui:launch"
mimose-runner = "runner.app:main"
